# graphic-1 — Logo & identité (Visual identity — brand)

Drop brand logo images here: `01-client.png`, `02-client.png`, …

Optional:

- `cover.png` — card thumbnail (if different from slide 01)
- `labels.json` — display names in the playlist

Then run from project root:

```bash
node scripts/build-portfolio-playlists.mjs
```
