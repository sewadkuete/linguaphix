# video-2 — Social handles

**Cover:** TikTok profile URL in `cover.url` (current: @anguillessousdroit).

Add video clips as `01-reel.mp4`, … — they appear in the modal playlist; `cover.url` stays as the card cover.

```bash
node scripts/build-portfolio-playlists.mjs
```
